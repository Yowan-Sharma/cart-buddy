import 'package:flutter/material.dart';

class AppColors {
  // Primary colors from logo
  static const Color primary = Color(0xFF134E48); // Dark Teal (Text)
  static const Color secondary = Color(0xFFF9B04D); // Orange (Smile/Person)
  static const Color accent = Color(0xFF5EADA1); // Aqua (Cart)
  
  // Background and surface colors
  static const Color background = Color(0xFFF0F8F7); // Light Mint
  static const Color surface = Colors.white;
  
  // Utility colors
  static const Color textMain = Color(0xFF134E48);
  static const Color textSub = Color(0xFF6B7280);
}
